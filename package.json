{
    "name": "preact-firestore",
    "version": "0.0.1",
    "scripts": {
        "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
        "build": "cd public && preact build --no-prerender",
        "serve": "cd public && preact build --no-prerender && preact serve",
        "dev": "cd public && preact watch",
        "deploy": "firebase deploy",
        "deploy-hosting": "cd public && preact build --no-prerender && firebase deploy --only hosting",
        "deploy-functions": "firebase deploy --only functions"
    },
    "dependencies": {
        "firebase": "^7.24.0"
    }
}
